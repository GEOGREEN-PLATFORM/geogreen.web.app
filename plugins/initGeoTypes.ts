// import { useMainStore } from "~/store/main";
// export default defineNuxtPlugin(async (nuxtApp) => {
//     const store = useMainStore();
//     store.problemAreaTypes = await $fetch(`${store.apiGeospatial}/geo/dict/problem-area-types`);
//     store.landTypes = await $fetch(`${store.apiGeospatial}/geo/dict/land-types`);
//   });
